# Cleanup Notes

This repo was trimmed down to the frontend-only browser game before the GitHub upload.

## Removed items

- `main.OLD`: historical backup of older gameplay logic.
- `style.OLD`: historical CSS backup.
- `counter.js`: unused Vite starter helper not used by the game.
- Python backend/auth scaffolding under `src/`
- Keycloak local-development files (`.env.example`, `docker-compose.yml`, `docs/keycloak-dev.md`)
- Python packaging files (`pyproject.toml`, `poetry.lock`)
- Empty root `Makefile`

